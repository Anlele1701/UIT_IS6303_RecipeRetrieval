__pycache__/
*.pyc
.venv/
venv/
data_cache/
# experiments/ is deliberately not ignored: the query set defines the ground
# truth behind docs/08-11, and the per-query records let every table and
# error-analysis card be regenerated without re-running retrieval.
.DS_Store
.env
